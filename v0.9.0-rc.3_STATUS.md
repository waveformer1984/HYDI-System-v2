# v0.9.0-rc.3 Release Candidate Status

## Relationship to rc.2

- `v0.9.0-rc.2` is tagged at `c9652b0`.
- `v0.9.0-rc.3` is tagged at `c8aaaaa` or the version-bump commit that follows it.
- `v0.9.0-rc.3` includes a cryptographic signature-verification fix not present in `v0.9.0-rc.2`.
- All evidence from `v0.9.0-rc.2` is still valid for the unchanged code, but the final release gates (24-hour soak and clean-machine deployment) must be executed against `v0.9.0-rc.3`.

## What Changed in rc.3

- `SignatureVerifier` now uses real Ed25519 signing/verification.
- `computeDigest()` now recursively canonicalizes nested objects.
- Forged signatures and tampered capabilities are rejected.
- The fix corrected inaccurate "0 findings" statements in `SECURITY_REVIEW.md` and `RC2_GO_NO_GO_REPORT.md`.

## Validation Run So Far

- `npm run typecheck` — PASS
- `npm run architecture-test` — PASS (100% score)
- `npm run rc1-test` — PASS
- `npm run rc2-test` — PASS

The full `npm test` suite passed at `c8aaaaa` per the commit message and prior full run.

## Missing Final Gates

- 24-hour wall-clock soak (`FINAL_24H_SOAK_REPORT.md`)
- Clean-machine deployment (`FINAL_CLEAN_DEPLOYMENT_REPORT.md`)

## Release Decision

`v0.9.0` promotion is **blocked** until both final gates are completed against `v0.9.0-rc.3`.
