# ISSUES FOUND

Discovered problems, tracked with status. Newest first. See `WORKLOG.md` for
the narrative of what was fixed and why; this file is the flat list.

---

## Fixed this session (2026-07-17, per-file review of ambiguous unbridged `api/**` routes)

Resolves `ROADMAP.md` P1 item 6 / `ISSUES_FOUND.md` #34: per-file review of
the 7 remaining ambiguous unbridged `api/**` routes, to tell "intentionally
superseded by `pages/api/**`" from "also just missing."

| # | Issue | File(s) | Status |
|---|-------|---------|--------|
| 49 | `api/ursula/status.js` — documented as a live endpoint (`README.md`, `CLAUDE.md`, `PROTOFORGE_SITREP.md`) but, like the routes bridged in #31, unreachable under `next dev`/`next start`. Public, read-only, no secrets (matches the already-bridged `health.js`/`mobile-status.js` observability pattern). Also still constructed its Supabase client at module load time — the exact crash-before-handler-runs bug already fixed in #32 for its siblings, just missed because this file wasn't touched in that pass. | `api/ursula/status.js` | **Fixed** — bridged into `pages/api/ursula/status.js`; switched to the lazy-construction-via-`Proxy` pattern from #32. Added `tests/unit/ursula-status.test.js` (6 tests) — this file had no prior coverage at all. |
| 50 | `api/events/stream.js` — already properly gated (`requireAuth`, rate-limited) and expected live by `hydi-mobile-protoforge.html`, but unreachable for the same reason as #49. | `api/events/stream.js` | **Fixed** — bridged into `pages/api/events/stream.js`. Already had test coverage (`tests/unit/events-stream.test.js`); reconfirmed passing. |
| 51 | `api/chat/route.js` — the documented "Universal chat router" (CLAUDE.md), HMAC-service-token gated + rate limited, ported as a reference implementation by `termux/hydi-chat-server.js`, and already unit-tested — but unreachable for the same reason as #49. Distinct in purpose from `pages/api/chat.ts` (Heidi's own streaming user-chat endpoint) — not a duplicate, confirmed by reading both. Bridging it surfaced a real latent bug: it imported `'../../lib/claude.js'` with an explicit `.js` extension, but only `lib/claude.ts` exists — Jest tolerates this via a `moduleNameMapper` rule (`jest.config.js`), but Next.js's webpack build has no equivalent and failed with `Module not found`. Every other `pages/api` consumer of the same file (e.g. `pages/api/chat.ts`) already imports it extensionless. | `api/chat/route.js` | **Fixed** — bridged into `pages/api/chat/route.js`; changed the import to extensionless `'../../lib/claude'`, matching the established convention. Verified with a real `npm run build` and the existing `tests/unit/chat-route-rezonate.test.js`. |
| 52 | `api/ws/route.js` — not a real WebSocket implementation, just a static JSON body describing hardcoded `ws://localhost:3005/ws/*` URLs matching no listener in this codebase. Confirmed zero references anywhere (no `require`/`import`, no route table entry, no client call). | `api/ws/route.js` | **Fixed** — archived to `archive/dead-ws-placeholder/` with a README, matching the established archival convention (`archive/dead-keymaker-and-break-glass-prototypes/`). |
| 53 | `api/heidi/route.js` — has **zero authentication** (no `requireAuth`, no service token — just a bare rate limit), yet accepts a `switch_model` action and forwards arbitrary user prompts to a cloud LLM fallback (Anthropic/OpenAI) if those keys are ever configured — the exact unauthenticated-cost/DoS-vector anti-pattern already fixed elsewhere in this audit trail (e.g. #29). It also duplicates functionality the already-live `pages/api/chat.ts` already covers via `lib/heidi-agent.ts` / local-model fallback in `src/models/`. Only reference anywhere in the repo is a self-documentation string in `api/agent-manager/agents.js`'s `AGENT_REGISTRY`; no test exists for it. | `api/heidi/route.js` | **Not fixed — left unbridged, flagged.** Given it's unauthenticated and appears superseded rather than uniquely functional, bridging it as-is would introduce a live vulnerability. Whether to add auth and keep it, or delete it as a superseded duplicate, is a product decision (same class of call as the 4-parallel-Stripe-implementations item in `ROADMAP.md` #5) — not guessed at here. |
| 54 | `api/client-dashboard.js` — has **zero authentication**, returns full per-project financial ledger data (gross/fees/net/payouts) filtered only by an attacker-controlled `?project=` query string — no ownership or identity check at all. Confirmed **currently unreachable and uncalled**: `dashboard/client-view.html` (its only plausible frontend) has the real `fetch('/api/client-dashboard?project=...')` call commented out in favor of hardcoded mock data. | `api/client-dashboard.js` | **Not fixed — left unbridged, flagged.** No live exploitation path today since nothing calls it and it isn't bridged, but it must not be bridged or wired up until it gains a real auth/ownership check — do not treat "currently dead" as "safe to bridge later without changes." |
| — | `api/local-model.js` was included in the original ambiguous-routes list (`ISSUES_FOUND.md` #34) but is not actually a route — it exports `{ LocalModelClient, HeidiLocalHandler }` with no default handler, so it could never have been invoked as an API endpoint regardless of bridging. It's a plain library required by `api/heidi/route.js` (see #53); no action needed beyond noting the list entry was a documentation artifact, not a real gap. | `api/local-model.js` | **Investigated, not a bug.** |
| 55 | **Follow-on to #51, found the same day by a parallel session.** `components/song-composer/CopilotPanel.tsx`'s `sendMessage` POSTed to `/api/chat/route` with `{ message, system: buildSystemPrompt() }` — a freeform system-prompt string, not one of the 8 fixed dispatch keys (`'ursula'`, `'heidi'`, `'cascade'`, ...) `api/chat/route.js`'s handler actually accepts, and with no HMAC `x-hydi-service-token` (which a browser has no safe way to compute — it's derived from a server-only secret). Bridging `api/chat/route.js` (#51) does not fix this: the contract mismatch and the auth requirement both remain. The Song Composer's AI copilot panel has never worked in production. | `components/song-composer/CopilotPanel.tsx` | **Fixed** — repointed at the real, live, unauthenticated `/api/chat` (`pages/api/chat.ts`, the same SSE `{message, session_id, user_id}` contract `pages/index.tsx`'s Heidi chat already uses successfully), added a per-mount `session_id`, and folded the song-context system prompt into the message text (the shared `/api/chat` contract has no separate system-prompt param). |

---

## Fixed this session (2026-07-16, follow-up audit)

| # | Issue | File(s) | Status |
|---|-------|---------|--------|
| 48 | **Follow-up to #36.** `api/rezonate/route.js`'s `get_project`, `list_tracks`, and `add_track` performed **no ownership check at all** — not "trusts an unverified `x-user-id` header," simply nothing: any caller holding a valid `rezonate:manage` device/service credential could read or write any project/track by id regardless of who it belonged to. Only `list_projects` and `create_project` referenced `userId` at all. Found while reviewing the identity-hardening roadmap item before starting it — the review also surfaced that this codebase has two separate, unconnected identity scaffolds (the working HMAC device/service token system in `lib/auth/deviceAuth.js`, and dormant Supabase Auth scaffolding — `rezonate_projects.user_id REFERENCES auth.users(id)` with correct RLS policies, plus unwired `lib/middleware.ts` session-refresh boilerplate — with no login/signup UI anywhere and the route using the service-role key, which bypasses RLS regardless). Deciding which of those becomes the real per-user identity source is a separate, larger product decision, tracked in `ROADMAP.md`; this fix only closes the immediate no-check gap using whatever `userId` the route already has. | `api/rezonate/route.js` | **Fixed** — added `projectBelongsToUser()`, checked before all three actions; a project belonging to a different `userId` (or missing entirely) now responds 404 instead of proceeding. Matches the existing posture elsewhere in this route: when no `x-user-id` header is present at all, still unscoped (unchanged, separate issue). Added 6 new tests to `tests/unit/rezonate.test.js`; also switched that file's `beforeEach` from `jest.clearAllMocks()` to per-mock `.mockReset()` on the shared chain mocks, since `clearAllMocks()` doesn't clear queued `mockResolvedValueOnce` values and a test that errored mid-chain was leaking stale queued values into the next test. | **Follow-up to #44.** `processAuthentication()` still simulated a successful authentication (and issued a real JWT for it) for any submitted email — the always-succeeds stub flagged in #44 was left open on 2026-07-15 pending a credential-schema decision. `checkTokenPermission()` had the same shape of problem: it returned `true` unconditionally with no RBAC implementation behind it, so any validly-signed JWT would pass every permission check in `validateToken()`. | `workers/SecurityIdentityWorker.js` | **Fixed — fails closed.** `processAuthentication()` now unconditionally logs every `auth.attempt` as rejected and never issues a token, since the payload it receives (`email`/`ip_address`/`user_agent`) carries no credential to check in the first place — this codebase has no password-based user schema at all (identity here is API-key based, see `src/middleware/keymaker.js`). `checkTokenPermission()` now returns `false` by default. **Real credential verification is still not implemented** — deciding how a legitimate caller should actually authenticate through this worker (extend the payload to carry an API key checked against `keymaker_keys`, a new password-based flow, or retiring this queue-based path in favor of the already-live `Keymaker` Express middleware) is a product/architecture decision left to the maintainer, not guessed at here. This worker still isn't started by any confirmed-live entry point (unchanged from #44). Extended `tests/unit/security-identity-worker-fail-closed.test.js` with regression coverage for both fail-closed behaviors. |

---

## Fixed this session (2026-07-15, production readiness audit — same day, third pass)

Resolves the "URGENT: confirm what's actually deployed" item from `ROADMAP.md`
and issue #33 below. Full architecture findings in `DEPLOYMENT.md`.

| # | Issue | File(s) | Status |
|---|-------|---------|--------|
| 38 | **Resolves #33 (High).** Payment-critical routes were confirmed unreachable under this app's real serving path (`next dev`/`next start`, which only ever serves `pages/api/**`) — meaning checkout could not be started and neither Stripe webhook handler could receive events. | `api/checkout.js`, `api/stripe-connect-webhook.js`, `api/webhooks/stripe.js` | **Fixed** — bridged into `pages/api/checkout.js`, `pages/api/stripe-connect-webhook.js`, `pages/api/webhooks/stripe.js` using the same `export { default } from ...` pattern already established for the 16 routes bridged in the prior pass. Verified with a real `npm run build` (all three appear in the Next.js route table) and Jest regression tests. |
| 39 | `api/checkout-v2.js` was a near-byte-identical duplicate of `api/checkout.js` (same logic, trivial formatting differences), with zero code references anywhere. | `api/checkout-v2.js` | **Fixed** — deleted. `api/checkout.js` is canonical. |
| 40 | **Critical latent bug, would have blocked #38 even after bridging**: `api/webhooks/stripe.js` set `module.exports.handler = async function(req, res) {...}` (the Vercel/Next.js-style entry point) and then immediately overwrote it with `module.exports = { handleStripeWebhook, SERVICE_TIERS }` a few lines later — silently dropping the handler. `require('api/webhooks/stripe.js')` never returned a callable function, so it could never have worked as an API route regardless of routing. Had zero test coverage (the existing `stripe-webhook.test.js` tests a *different* file, `src/webhook-handlers/stripe-webhook.js`). | `api/webhooks/stripe.js` | **Fixed** — restructured so `module.exports` is the callable handler itself, with `.handleStripeWebhook`, `.SERVICE_TIERS`, and `.config` attached as properties (preserves the existing `stripe-webhook-server.js` standalone Express consumer, which destructures `{ handleStripeWebhook }`). Added `tests/unit/webhooks-stripe.test.js` (9 tests) — this file had no prior coverage at all. |
| 41 | **Critical latent bug**: both `api/stripe-connect-webhook.js` and `api/webhooks/stripe.js` read `req.body` directly for Stripe signature verification, but Next.js does **not** auto-populate `req.body` when `bodyParser: false` is set (required for raw-body signature verification) — the route receives the raw unconsumed request stream and must read it itself. Even after fixing #38/#40, both webhooks would have thrown `No signatures found matching the expected signature` (or worse, an empty-body error) on every real Stripe delivery. Confirmed by checking Next.js's actual Pages Router behavior and cross-referencing a working reference implementation already dormant elsewhere in the repo (`hydi-monitor-deploy/pages/api/webhook.js`, which correctly uses `micro`'s `buffer(req)`). The existing unit test for `stripe-connect-webhook.js` didn't catch this because its fake `req` object hands `body` in directly rather than going through a real stream. | `api/stripe-connect-webhook.js`, `api/webhooks/stripe.js` | **Fixed** — added `lib/get-raw-body.js` (buffers the request stream; passes through unchanged if `req.body` is already a Buffer/string, so the existing Express-server and unit-test call paths are unaffected) and wired it into both handlers. `tests/unit/get-raw-body.test.js` (4 tests) covers all three paths (Buffer passthrough, string passthrough, real stream). |
| 42 | `src/api/services/index.js`'s `POST /subscriptions/checkout` computed `customerId = req.user?.stripeCustomerId \|\| bodyCustomerId \|\| 'cus_test_default'` with the comment `// For testing: allow customerId in body when auth is disabled` — since `authenticateToken` is commented out everywhere in this router (its module, `middleware/auth.js`, doesn't exist), any unauthenticated caller could create a Stripe checkout session against an arbitrary customer ID of their choosing. | `src/api/services/index.js` | **Fixed** — now returns 401 when there's no authenticated `req.user`, matching the "fail closed" requirement; the body-supplied fallback is removed. This router's production reachability is itself unclear (see open item below), but the fix is cheap, safe, and correct regardless of whether/when it's deployed. Added `tests/unit/service-bundle-checkout.test.js`. |
| 43 | `generate-break-glass-jwt.js` (an operator CLI helper for testing the break-glass Edge Functions) signed JWTs with `process.env.KEEPER_BREAK_GLASS_TOKEN \|\| 'fallback-secret'` — silently using a public, well-known secret with no warning if the env var was unset. The verifier side (`supabase/functions/keeper-break-glass{,-simple}`) was already fixed in the prior session to fail closed (503) in this situation, so the practical impact today is a false-confidence UX bug rather than a live bypass — but it's the same anti-pattern and a regression risk if the verifier side is ever misconfigured back to permissive. | `generate-break-glass-jwt.js` | **Fixed** — throws immediately with a clear message instead of silently signing with the fallback. |
| 44 | **Critical, if ever wired up**: `workers/SecurityIdentityWorker.js` — registered in `WorkerOrchestrator.js` (i.e. part of the live worker set once `WorkerOrchestrator`/`start-workers.js` is started) — signs and verifies real auth JWTs (`processAuthentication`, `validateToken`) using `process.env.JWT_SECRET \|\| 'dev-secret-change-in-production'`. Anyone reading this public repo's source would know the exact fallback secret and could forge arbitrarily-privileged tokens if `JWT_SECRET` were ever left unconfigured. Independently, `processAuthentication` currently **always succeeds** regardless of the submitted credentials — the code's own comment says *"In a real system, you would verify credentials against a user database... For now, we'll simulate a successful authentication for demo purposes."* No code path found in `package.json` scripts, `ecosystem.config.js`, `scripts/start-hydi.js`, or `boot.config.json` currently starts `start-workers.js`/`WorkerOrchestrator`, so this worker does not appear to be part of the confirmed-live pages/api surface today — but that reachability is unconfirmed (same "PM2 on a real host we can't inspect" caveat as the rest of the PM2 fleet, see `DEPLOYMENT.md`). | `workers/SecurityIdentityWorker.js` | **Partially fixed.** The hardcoded-secret fallback is fixed — `initialize()` now throws `Missing JWT_SECRET` if unset, matching the adjacent Supabase-credentials check already in the same function. **The always-succeeds authentication stub is a separate, deeper issue left open** — real credential verification requires a user/password schema decision this audit shouldn't guess at. **Do not wire this worker into any real authentication path until `processAuthentication` actually checks credentials.** Added `tests/unit/security-identity-worker-fail-closed.test.js`. |
| 45 | Same fallback-secret anti-pattern in the separate `apps/ursula-frontend` sub-app: `EnforcementBoundary` (a control surface explicitly documented as "cannot be influenced by any upstream system at runtime") gated `lockBoundary()`/`unlockBoundary()` on `process.env.BOUNDARY_SECRET \|\| 'default_boundary_secret'` — a public fallback that would let anyone defeat the boundary's entire purpose if the env var were unset. | `apps/ursula-frontend/runtime/enforcement-boundary/index.js` | **Fixed** — no fallback; `lockBoundary()`/`unlockBoundary()` already reject a `null`/missing `externalAuth`, so this now fails closed instead of accepting a well-known default. |
| 46 | Quantified the git-history exposure from #20/#21 (previous session): the same secret-shaped patterns (Supabase service-role JWT, `sk_live_`/`rk_live_`/`whsec_` Stripe keys) still appear **399 times** across `git log --all -p`, and two real (non-example) `.env.backup` / `.env.production` files — confirmed to have included `STRIPE_SECRET_KEY` and `SUPABASE_SERVICE_ROLE_KEY` variable names — were committed and later untracked (commit `56c0e7c`), but remain permanently recoverable from history. This does not change the remediation (rotation, not history rewriting, is the correct fix per `SECURITY_PROTOCOL.md` — rewriting shared branch history is a separate, higher-risk decision for the maintainer) but confirms the "must rotate" status is not stale/theoretical. | git history (not fixable in the working tree) | **Not fixed — confirms open item.** See `SECURITY.md` and the manual operator actions in `DEPLOYMENT.md`. |

### Investigated, not fixed (out of scope for this pass)

- **`modules/keymaker-core.js`** has the same `process.env.KEYMAKER_SECRET \|\| 'default-secret-change-me'` fallback pattern, but is orphaned — no file in the repo `require()`s it (the live `Keymaker` used by `src/server.js` is the unrelated `src/middleware/keymaker.js`, which has no such fallback). Not fixed since it's unreachable; flagged for a future dead-code cleanup pass rather than left silently as-is.
- **`emergency/break-glass-implementation.js`** and **`keeper/emergency/break-glass.js`** are unreferenced (no `require()` anywhere) prototype implementations of break-glass token verification with hardcoded HMAC secrets (`'break-glass-time-secret'`, `'break-glass-secret'`) directly in source, each self-documented as non-production (`// In production, use proper secret`). Dead code, superseded by the live `supabase/functions/keeper-break-glass` Edge Function (already hardened in the prior session). Recommend archiving in a follow-up so they can't be mistaken for the live implementation.
- **At least four independent Stripe checkout/webhook implementations exist beyond the ones fixed this session**, none wired into the confirmed-live `pages/api` surface: `src/webhook-handlers/stripe-webhook.js` (class-based, targets `users`/`api_keys` tables — an API-key-per-tier SaaS model, referenced only by its own unit test), `src/api/services/index.js`'s `/subscriptions/checkout` + `/webhooks/stripe` (Express router mounted into `src/server.js`, whose own production status is unconfirmed — see `DEPLOYMENT.md`), the standalone `stripe-webhook-server.js` (a dedicated Express micro-server not referenced by any `package.json` script), and a fully separate stale sub-deployment at `hydi-monitor-deploy/` (own `package.json`/`vercel.json`/`netlify.toml`/built `.next`, superseded). None were modified — consolidating or deleting any of these requires a product decision on which billing model (Connect sub-accounts vs. per-tier API keys vs. the Ursula service bundle) is actually current, which this audit cannot guess. Documented in full in `DEPLOYMENT.md`.
- **`src/api/services/index.js`'s remaining routes** (`/services`, `/services/:id/execute`, `/usage`, `/bundle`, `/analytics`) are broken independent of auth: `SubscriptionManager` never constructs `this.serviceBundle` (`UrsulaServiceBundle` is commented out — `// Temporarily disabled due to syntax errors`), so every route that touches `subscriptionManager.serviceBundle` throws `Cannot read properties of undefined`. Not attempted — fixing the underlying `UrsulaServiceBundle` syntax errors is a separate, unscoped task.

---

## Fixed this session (2026-07-15)

| # | Issue | File | Status |
|---|-------|------|--------|
| 1 | `npm run lint` failed to parse ~60 files across `pages/`, `components/`, `lib/`, `src/` — the project's `.eslintrc.json` had no Next.js/TypeScript parser configured, so the ESLint gate was silently non-functional for the entire frontend and much of the TS backend. | `.eslintrc.json` | **Fixed** — scoped `next/core-web-vitals` override added for `pages/`, `components/`, `lib/`, `hooks/`. |
| 2 | Fixing #1 crashed `next lint` with `TypeError: expand is not a function`. Caused by `package.json`'s `"overrides": {"brace-expansion": ">=5.0.6"}` force-pinning a breaking-change major version (v5, new export shape) onto every `minimatch@3.x` consumer in the ESLint toolchain, which expects the old `1.x` API. | `package.json` | **Fixed** — removed the blanket override; each consumer now resolves its own compatible (and already CVE-safe) major version. Verified `npm audit` stays at 0 vulnerabilities. |
| 3 | `strategy` and `input` referenced inside a `catch` block after being `const`-declared inside the paired `try` block (out of scope) — any inference failure threw a fresh `ReferenceError` from the error handler instead of surfacing the real error, and local-model fallback never ran. | `src/models/HybridModelStack.js` | **Fixed** — hoisted with `let` above the `try`. |
| 4 | Typo `worSourceSource` (should be `worstSource`) in a template literal, silently caught — the lead-source-underperformance adaptation suggestion was never returned when it should fire. | `src/control/OutcomeValidator.js:235` | **Fixed**. |
| 5 | Two class methods both named `checkModelHealth` (aggregate no-arg version + per-model version); the second silently shadowed the first, so the periodic heartbeat's aggregate check, failure tracking, `recoverFailedModels()`, and `storeHeartbeatMetrics()` were 100% dead code. The live heartbeat interval was actually calling the per-model checker with `modelId=undefined`. | `src/models/heartbeat.js` | **Fixed** — renamed per-model method to `checkSingleModelHealth`. |
| 6 | Once #5 was fixed and the aggregate method became reachable, it hit a second latent bug: `this.failedModels` constructed as a `Set` but used with `Map`-only methods (`.get()`, `.set()`, `forEach((count, modelId) => …)`). Would have thrown `TypeError` on every check cycle with at least one failed model. | `src/models/heartbeat.js` | **Fixed** — changed to `Map`. |
| 7 | `POST /cascade/quarantine/:eventId/release` referenced undefined `approvedBy` instead of the destructured `approved_by`; endpoint threw on every call. | `src/server.js:1055` | **Fixed**. |
| 8 | All five `/keymaker/*` admin routes (status, issue/revoke key, validate, audit, kill-switch, break-glass) referenced an undefined `keymaker` object — the full `Keymaker` class existed at `src/middleware/keymaker.js` but was never imported/instantiated in `server.js` (only the separate, simpler `SimpleKeymaker` tier-gate was wired up). | `src/server.js` | **Fixed** — imported, instantiated, and mounted `Keymaker`'s middleware alongside `SimpleKeymaker`'s. |
| 9 | 6 orphaned files under `src/` used ES module `import`/`export` syntax with no `.mjs` extension or `"type": "module"`, and were unreachable via `require()` from any live code path (`src/net/net.js`, `src/server-clean.js`, `src/services/persistence.js`, `src/db/dbRouter.js`, `src/db/local.sqlite.js`, `src/lib/supabaseClient.js`). | `src/**` | **Fixed** — archived to `archive/src-esm-orphans/` with a README explaining the reachability analysis, matching the existing archival convention. |
| 10 | `tests/unit/hydi-v3/DistributedCompute.test.js`'s `detects node timeout` and `tests/unit/hydi-v3/HeartbeatSystem.test.js`'s `detects missing heartbeat` tests were flaky — each raced a fixed `setTimeout` against the engine's own interval-based health check, both landing near the same wall-clock instant. | `tests/unit/hydi-v3/{DistributedCompute,HeartbeatSystem}.test.js` | **Fixed** — replaced the fixed sleeps with `Promise.race` against the actual event, generous 2s ceiling. |
| 11 | `npm run lint` was not wired into CI (`unit-tests.yml` only ran `npm test`), which is exactly how #1 went unnoticed for an unknown period. | `.github/workflows/unit-tests.yml` | **Fixed** — added a `Run ESLint` step before the test step. |
| 12 | 9x `no-case-declarations` (lexical `const`/`let` directly in a `switch case` without block braces) in `src/HYDISystem.js`, `pages/api/revenue/index.js`, `src/control/HeidiControlPlane.js`. | see files | **Fixed** — wrapped each case body in `{ }`. One instance in `HeidiControlPlane.js` was a real latent bug (see WORKLOG #6 for detail): two sibling cases would redeclare the same `const` name in shared scope had the author not already hand-rolled a `2`-suffix workaround; removed the workaround now that proper block scoping is in place. |
| 13 | 3x `no-prototype-builtins` — `.hasOwnProperty()` called directly on a plain object instead of `Object.prototype.hasOwnProperty.call(...)`, which throws if the target was ever created with `Object.create(null)`. | `lib/ActionParser.ts`, `lib/ModelManager.ts`, `src/enforcement/RuntimeEnforcer.js` | **Fixed**. |
| 14 | `lib/protoforge/policy-engine.js:257` had an empty `catch (_) {}` (`no-empty`) guarding per-callback failures during ProtoForge's realtime policy hot-reload. | `lib/protoforge/policy-engine.js` | **Investigated — confirmed intentional, not a bug** (isolates one bad reload callback from breaking the others). Gave it a named `err` + `console.warn` + explanatory comment so it's no longer a bare empty block. |
| 15 | `pages/index.tsx`, `pages/funding.tsx`, `pages/test-simple.tsx` used raw `<a href="/...">` for internal navigation instead of Next.js `<Link>`, forcing a full page reload instead of client-side routing (`@next/next/no-html-link-for-pages`). | `pages/*.tsx` | **Fixed** — swapped to `<Link>`. |
| 16 | 2x `react/no-unescaped-entities` — literal `"` in JSX text. | `components/song-composer/{MidiStatusBar,SongStructure}.tsx` | **Fixed** — escaped to `&quot;`. |
| 17 | `.githooks/pre-push` — the local safety-net hook CLAUDE.md describes as load-bearing ("day-to-day development has a source of truth that doesn't depend on GitHub Actions being up") — was committed to git with mode `100644` (non-executable), so it has silently no-op'd on every push, for every clone of this repo, since it was added. `git push` in this session printed `hint: The '.githooks/pre-push' hook was ignored because it's not set as executable.` | `.githooks/pre-push` | **Fixed** — `chmod +x`; git now tracks it as `100755`. Also added a `lint` step to the hook (it only ran typecheck + test before) now that lint is meaningful and CI-gated. |
| 18 | ~150 `no-unused-vars` ESLint *warnings* across `src/`, `lib/`, `components/` (now that lint actually runs) — unused function args, unused destructured values, an unused `useEffect` import. | many, see `npm run lint` output | **Open — Low severity.** Cosmetic/dead-parameter cleanup; large surface area, left out of this pass to keep the diff focused on correctness bugs. Good candidate for a dedicated follow-up pass, ideally file-by-file rather than a mechanical sweep since some `context`/`task` params are part of a shared handler signature contract. |
| 19 | `tests/unit/hydi-v3/WatchdogSupervisor.test.js` uses the same fixed-`setTimeout`-vs-own-interval pattern as the two tests fixed in #10, but was not observed to be flaky across several runs in this session. | `tests/unit/hydi-v3/WatchdogSupervisor.test.js` | **Open — Low severity.** Same latent race risk as #10; left alone since it isn't currently failing and the mission favors minimal, verified diffs. Worth applying the same `Promise.race` treatment proactively in a follow-up. |

---

## Fixed this session (2026-07-15, security pass — same day, later)

Full narrative in WORKLOG.md's "Security incident response + route
reachability gap" entry. Flat list here for tracking.

| # | Issue | File(s) | Status |
|---|-------|---------|--------|
| 20 | **CRITICAL, active incident**: live Supabase `service_role` JWT (full RLS bypass) hardcoded in 21 tracked files, including baked directly into a `pg_cron` migration (`supabase/migrations/20260426122000_action_worker_cron_schedule.sql`) — meaning it was very likely also stored in the live database's `cron.job` table, not just in source control. | 21 files, see WORKLOG | **Partially fixed — rotation still required.** New Vault-backed migration + `add-vault-secrets.js` rewrite + secret scrubbed/redacted from all 21 files (20 deleted as dead one-off scripts, 1 redacted). **The key itself must still be rotated in the Supabase dashboard — I have no authenticated access to do this. Editing files does not undo git-history exposure.** |
| 21 | **CRITICAL**: live Stripe restricted key (`rk_live_...`) + two live Stripe webhook signing secrets (`whsec_...`) hardcoded in `vercel-env-checklist.md`, `comprehensive-audit-report.md`, and `test-webhook.js`. | 3 files | **Partially fixed — rotation still required.** Redacted/deleted. **Must be rotated in the Stripe dashboard.** |
| 22 | `keeper-break-glass` and `keeper-break-glass-simple` Edge Functions fell back to a hardcoded, now-public string (`'fallback-secret'` / `'break-glass-secret-test'`) as the auth secret whenever `KEEPER_BREAK_GLASS_TOKEN` was unconfigured — anyone could forge a valid break-glass token (escalates a safety circuit breaker). | `supabase/functions/keeper-break-glass{,-simple}/index.ts` | **Fixed** — both fail closed (503) instead of authenticating against a fallback. |
| 23 | `api/song-composer/songs.js` — unauthenticated `DELETE` on the shared `actions` table by raw id (IDOR: could delete unrelated rows, e.g. agent-manager tasks). | `api/song-composer/songs.js` | **Fixed** — `requireAuth` gate + scoped delete to `task_name='song_composition'`. |
| 24 | `api/agent-manager/tasks.js` — unauthenticated task dispatch (triggers real agent execution) and cancel/retry; sibling `control.js` already had `requireAuth`, this one was missed. | `api/agent-manager/tasks.js` | **Fixed** — matches sibling's pattern. |
| 25 | `api/agent-manager/agents.js` — unauthenticated read of aggregated agent/task stats; inconsistent with its siblings. | `api/agent-manager/agents.js` | **Fixed** — `requireAuth('worker:view')`. |
| 26 | `api/hydi/sync.js` — unauthenticated RPC triggers (`auto_heal_from_trends`, `analyze_health_trends`, `evaluate_system_escalation`) + arbitrary event-bus injection. | `api/hydi/sync.js` | **Fixed** — `status:view` (GET), new `hydi_sync:trigger` permission (POST). |
| 27 | `api/rezonate/route.js` — unauthenticated project/track CRUD + task dispatch. | `api/rezonate/route.js` | **Fixed** — new `rezonate:manage` permission gates the whole handler. Per-user data scoping still trusts the `x-user-id` header — see #29 below, already the maintainers' own top roadmap item, not newly introduced by this fix. |
| 28 | `api/life-flow/route.js` — unauthenticated Deep Life Architect request processing. | `api/life-flow/route.js` | **Fixed** — new `life_flow:manage` permission. Deliberately left unreachable (see #31). |
| 29 | `api/song-composer/generate.js` — unauthenticated LLM-backed generation, cost/DoS vector. | `api/song-composer/generate.js` | **Fixed** — `requireAuth` + tightened 10/min rate limit (default 60/min). |
| 30 | `stripe-transfer-payout` and `stripe-connect-admin` Edge Functions — move/manage real money and Stripe Connect accounts with zero authorization check; both absent from `supabase/config.toml`'s `verify_jwt` table (silently fell back to the platform default, which only proves *a* valid JWT, and the public anon key satisfies that). | `supabase/functions/stripe-{transfer-payout,connect-admin}/index.ts`, `supabase/config.toml` | **Fixed** — both now require the JWT's `role` claim to be `service_role` (Supabase's platform layer already verified the signature); added explicit `verify_jwt = true` entries to `config.toml`. |
| 31 | **Major**: the entire top-level `api/` directory (~30 files) was unreachable via the app's actual runtime (`next dev`/`next start` only serves `pages/api/*`; a bare top-level `api/` is a Vercel-only convention, and Vercel deployment is explicitly disabled per CLAUDE.md). Only 2 of ~30 files had any `pages/api/` counterpart. `.github/workflows/health-monitor.yml`'s "health check" never made an HTTP request to verify reachability, so this was invisible to CI. | `api/**`, `pages/api/**` | **Partially fixed.** Added `pages/api/**` re-export bridges for 16 low/no-risk routes (pure observability: `health.js`, `mobile-status.js`; the already-authenticated "mobile-ops" cluster + song-composer + rezonate + hydi-sync), each independently verified live against a running dev server. **Deliberately not bridged** — see #32-#34. |
| 32 | Fixing #31 for `health.js`/`mobile-status.js` surfaced a robustness bug: both (and 3 others touched this session) constructed their Supabase client at module load time, so a missing env var crashed the whole module before the handler's own graceful-degradation try/catch could run. | `api/health.js`, `api/mobile-status.js`, `api/song-composer/generate.js`, `api/hydi/sync.js`, `api/rezonate/route.js`, `api/life-flow/route.js` | **Fixed** — switched to the lazy-construction-via-`Proxy` pattern already established in `api/agent-manager/control.js`. |

---

## Open / not addressed this session

| # | Issue | File(s) | Severity | Notes |
|---|-------|---------|----------|-------|
| 33 | Payment-critical `api/` routes never bridged into reachability: `checkout.js`, `checkout-v2.js`, `stripe-connect-webhook.js`, `webhooks/stripe.js`, `webhooks/stripe-test.js`. | `api/{checkout,checkout-v2,stripe-connect-webhook}.js`, `api/webhooks/*` | **Fixed 2026-07-15 (third pass) — see #38-#41 above.** `checkout.js`, `stripe-connect-webhook.js`, and `webhooks/stripe.js` are now bridged into `pages/api/` and confirmed reachable via a real `npm run build`; two additional latent bugs that would have broken them even once reachable (a clobbered export, and missing raw-body reading for Stripe signature verification) were found and fixed in the same pass. `checkout-v2.js` was deleted as an exact duplicate rather than bridged. `webhooks/stripe-test.js` (a diagnostic-only endpoint with no real function) was deliberately left unbridged — still dead under this deployment model, low risk, not payment-critical. |
| 34 | Remaining ambiguous unbridged `api/` routes: `chat/route.js`, `heidi/route.js`, `client-dashboard.js`, `ursula/status.js`, `events/stream.js`, `ws/route.js`, `local-model.js`. `revenue.js` and `traces.js` were left alone since `pages/api/revenue/*` and `pages/api/traces.js` already exist and are presumably the live versions. | `api/**` | Medium | **Resolved 2026-07-17 — see #49-#54 above.** `chat/route.js`, `ursula/status.js`, and `events/stream.js` bridged (confirmed genuinely distinct from their `pages/api` siblings, not superseded). `ws/route.js` archived as confirmed-dead. `heidi/route.js` and `client-dashboard.js` deliberately left unbridged — both lack any auth and bridging them as-is would be a new vulnerability, not a fix. `local-model.js` was never a route at all. |
| 35 | `api/life-flow/route.js` instantiates a `HYDISystem` and starts recurring hardware/software-polling timers (`hardwareInterval: 5000`, `softwareInterval: 10000`, `analysisInterval: 60000`) **at module load**, not per-request — unusual and risky for an API route file, since merely importing it (e.g. via a `pages/api` bridge) starts persistent background timers as a side effect. | `api/life-flow/route.js` | Medium | Auth was fixed (#28) but the route was deliberately left unbridged/unreachable pending a product decision on whether this module-load side effect is intentional and safe to activate. |
| 36 | `api/rezonate/route.js`'s per-action authorization still trusts the `x-user-id` request header as identity with no cryptographic verification — this session added a `requireAuth` *gate* (proves some valid device/service credential exists) on top, but the deeper per-user data scoping is unchanged. | `api/rezonate/route.js` | Medium | Already the maintainers' own documented top roadmap item ("Security: cryptographic identity verification" in ROADMAP.md's Near-term section) — not a new finding, just re-confirmed. Redesigning this is a bigger architectural task than a hardening pass should do unilaterally. |
| 37 | `AGENT_REGISTRY` in `api/agent-manager/agents.js` lists Rezonate's endpoint as `/api/rezonate`, but the actual file lives at `api/rezonate/route.js`, which (matching Vercel's own file-based routing convention, now that it's bridged) resolves to `/api/rezonate/route`, not `/api/rezonate`. | `api/agent-manager/agents.js` | Low | Pre-existing documentation/consistency nit, not introduced this session, not blocking. |

---

## Investigated, not a bug

- `lib/protoforge/policy-engine.js:257`'s empty catch — see #14 above.
- `keeper/vault/versioned-vault.js` and two `cleanup/*.ps1` scripts matched
  the new secret-scanner patterns but are false positives: the vault file
  uses a literal `"sk_live_"`-prefixed placeholder string (not a real key), and the cleanup
  scripts contain a secret-*detection* regex list (a scanning tool, not an
  instance of what it scans for) — see `tests/unit/no-hardcoded-secrets.test.js`'s
  allowlist for the exact reasoning per file.
- `public/client-dashboard.html`'s hardcoded Supabase anon key — this is
  the expected, by-design pattern (anon keys are meant to be public
  client-side, protected by RLS), materially different from the
  `service_role` leaks above. Not a bug.
