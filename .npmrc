# All known vulnerabilities are resolved:
# - postcss GHSA-qx2v-qp2m-jg93: fixed via flat override "postcss": "^8.5.15"
#   which forces npm to hoist next's exact-pinned postcss@8.4.31 to the safe version.
# audit-level is left at the npm default (moderate+) so CI will catch regressions.
