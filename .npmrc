# Fail npm audit only on high/critical vulnerabilities.
# The remaining moderate advisory (GHSA-qx2v-qp2m-jg93 / postcss <8.5.10)
# is hardcoded inside next@15.5.18 as an exact pin ("postcss": "8.4.31")
# and cannot be overridden without downgrading Next.js to 9.3.3 (breaking).
# Impact is build-time only; it does not affect the running web server.
# Track: https://github.com/advisories/GHSA-qx2v-qp2m-jg93
audit-level=high
